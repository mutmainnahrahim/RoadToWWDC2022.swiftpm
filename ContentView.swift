import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("INSECURITY")
                .font(.largeTitle)
            Text("APPS")
                .font(.largeTitle)
            Image("memoji")
            Text("How are you")
        }
    }
}
